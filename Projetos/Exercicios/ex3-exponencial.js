// Crie uma função que recebe dois parâmetros, base e expoente, e retorne a base elevada ao expoente.

// Exemplo de arrow armazenado em variável 
//const potencia = (x, y) => return x**y
function exp (x, y) {
    return x**y
}

//console.log(potencia(2, 3))
console.log(exp(2, 3))
